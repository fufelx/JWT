CREATE TABLE users (
                                 "id" BIGINT PRIMARY KEY,
                                 "ip" character varying(15) NOT NULL,
                                 "refreshtoken" TEXT NOT NULL
);